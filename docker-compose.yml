services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7331:7331"
    volumes:
      - database:/app/storage
      - ./static:/app/static
    restart: unless-stopped
    env_file:
      - .env
    
volumes:
  database: